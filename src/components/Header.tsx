import React from "react";

const Header: React.FC = () => {
  return (
    <header style={{ width: "100%", display: "flex", justifyContent: "center", alignItems: "center", padding: "10px" }}>
      <h1>Tech Equipment Registry</h1>
    </header>
  );
};

export default Header;
